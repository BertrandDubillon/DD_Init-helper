const { createTheme } = require("@mui/material");

const theme = createTheme({
    palette: {
      primary: {
        main :"#1B262C", 
      },
      secondary: {
        main : "#0F4C75",
      },
    },
  });
  
  export default theme;