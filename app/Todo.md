// Logic Bar must be anchored in non mobile - done





// Add a reset all button somwhere to have a clean slate

// Reduce all font size

// NextTurn function must focus on the right name in the name list (useRef, forwardRef + useImperativeHandle)

// "Help" section explaining how the names NumberLetter work. Aka 1A 2B 3B etc...

// Saving current state to the LocalStorage. When to do that ?

// scroll a fixed number of pixel to 'jump line by line'